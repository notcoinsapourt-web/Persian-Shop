/** @type {import('next').NextConfig} */
const nextConfig = {
  // Permit the workspace browser to load development assets through its preview host.
  allowedDevOrigins: ['terminal.local'],
};
export default nextConfig;
